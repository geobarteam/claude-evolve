# Lineage

One row per generation, appended by the evolver runner (or by the owner after a manual revert). Status: `provisional` (< 14 days), `settled`, `reverted`, `rejected` (no commit made).

| Generation | Date | Score | Status | Summary |
| --- | --- | --- | --- | --- |
